[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "tritrust-gait-reproduction"
version = "2.0.0"
description = "Reconstructed TriTrust implementation with explicit evidence boundaries"
requires-python = ">=3.10"
[tool.setuptools.packages.find]
where = ["src"]
