# Source revision

The recorded commit identifies the newly reconstructed source snapshot, not the historical paper training code. Run configs additionally hash the executed Python modules. The bundle excludes data, checkpoints and the manuscript; extract the full archive to inspect execution artifacts.
